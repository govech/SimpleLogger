# ==============================================================================
# Android 模块级 .gitignore (app/)
# 作用范围：仅当前模块
# ==============================================================================

# ------------------------------------------------------------------------------
# 模块构建输出
# ------------------------------------------------------------------------------
/build

# ------------------------------------------------------------------------------
# 模块特定输出目录
# ------------------------------------------------------------------------------
/outputs/                   # 构建输出文件
/release/                   # 发布版本输出
/debug/                     # 调试版本输出

# ------------------------------------------------------------------------------
# 生成的文件
# ------------------------------------------------------------------------------
/generated/                 # 代码生成目录
/build/generated/           # 构建生成的文件

# ------------------------------------------------------------------------------
# 模块特定的敏感文件
# ------------------------------------------------------------------------------
# 模块特定的配置文件 (如果存在)
module-config.properties

# 模块特定的签名配置 (如果存在)
signing-config.json

# ------------------------------------------------------------------------------
# 测试相关
# ------------------------------------------------------------------------------
/reports/                   # 测试报告
/test-results/              # 测试结果

# ------------------------------------------------------------------------------
# 临时文件
# ------------------------------------------------------------------------------
*.log                       # 模块特定的日志文件
*.tmp                       # 模块特定的临时文件